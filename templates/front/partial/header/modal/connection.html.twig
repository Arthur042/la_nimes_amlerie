
<div class="modal fade" id="connectionModal" tabindex="-1" aria-labelledby="connectionModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-body">
                <h5 class="modal-title" id="connectionModalLabel">Identifiez-vous</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>

                <form  method="post">
                    <div class="mb-4">
                        <label for="emailConnexion" class="d-none">Adresse email</label>
                        <input type="email" class="form-control" id="emailConnexion" name="emailConnexion"
                            placeholder="Adresse email">
                    </div>
                    <div class="mb-4 passwordInput">
                        <label for="password" class="d-none">Mot de passe</label>
                        <input type="password" class="form-control" id="password" name="password"
                            placeholder="Mot de passe">
                        <span class="input-group-text hide" id="togglePassword"></span>
                    </div>

                    <button type="submit" class="lna_btn_main lna_btn_dark">Se
                        connecter</button>
                </form>

                <p>Si vous n'avez pas de compte : <a data-bs-toggle="modal"
                        data-bs-target="#inscriptionModal">Inscrivez-vous</a></p>
            </div>
        </div>
    </div>
</div>
