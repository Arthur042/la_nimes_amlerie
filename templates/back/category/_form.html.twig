<div class="container-fluid">
    <div class="mt-3 crudForm m-auto">
        {{ form_start(form) }}
            {{ form_widget(form) }}
        <button class="btn btn-success">{{ button_label|default('Ajouter') }}</button>
        {{ form_end(form) }}
    </div>
</div>